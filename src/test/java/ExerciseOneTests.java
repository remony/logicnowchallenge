import junit.framework.TestCase;


public class ExerciseOneTests extends TestCase {
    /*String[] op = new String[]{"mount", "testDB"};
    //Check that there is 2 arguments
    public void testCheckForArguments() {
        Exercisetwo exerciseTwo = new Exercisetwo();
        assertTrue(exerciseTwo.checkiftwoargs(op));
    }

    public void testCheckArgsForEmptyargs()   {
        Exercisetwo exerciseTwo = new Exercisetwo();
        assertTrue(exerciseTwo.checkifvalidargs(op));
    }

    //Check that the name of the mailstore is a string and not empty or spaces
    public void testCheckArgsForEmptyMailstoreArg()   {
        Exercisetwo exerciseTwo = new Exercisetwo();
        assertTrue(exerciseTwo.checkifvalidMailstoreArgs(op[1]));
    }

    //check that mount returns true (that the mount was successful)
    public void testStartMount()    {
        Exercisetwo exerciseTwo = new Exercisetwo();
        assertTrue(exerciseTwo.mount(op[1]));
    }

    //Checks that when issuing a mount request if the mailstore is already mounted it returns false
    public void testCheckIfMountReturnsFalseIfAlreadyStarted()  {
        Exercisetwo exerciseTwo = new Exercisetwo();
        exerciseTwo.mount(op[1]);
        assertFalse(exerciseTwo.mount(op[1]));
    }

    //check that dismount returns true (that the dismount was successful)
    public void testCheckIfMailstoreDismounts() {
        Exercisetwo exerciseTwo = new Exercisetwo();
        exerciseTwo.mount(op[1]);
        assertTrue(exerciseTwo.disMount(op[1]));
    }

    //Check that if a dismount returns false. (This means that the mailstore was not already mounted)
    public void testCheckIfMailStoreDismountFails() {
        Exercisetwo exerciseTwo = new Exercisetwo();
        exerciseTwo.mount(op[1]);
        exerciseTwo.disMount(op[1]);
        assertFalse(exerciseTwo.disMount(op[1]));
    }

    //Check that check reports a boolean value that says true or false
    public void testCheckIfCheckReturnsBoolean()    {
        Exercisetwo exerciseTwo = new Exercisetwo();
        exerciseTwo.mount(op[1]);
        assertTrue(exerciseTwo.checkIfMounted(op[1]));
        exerciseTwo.disMount(op[1]);
        assertFalse(exerciseTwo.checkIfMounted(op[1]));
    }*/
}
